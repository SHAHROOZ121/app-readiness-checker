{
  "buildCommand": "pnpm --filter @workspace/app-ready run build",
  "outputDirectory": "artifacts/app-ready/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
